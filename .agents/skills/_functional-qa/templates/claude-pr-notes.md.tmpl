# Suggested PR Notes

- Issue: {{issue_ref}}
- URL: {{issue_url}}
- Provider: `{{provider_display_name}}` (`{{provider_id}}`)
- Suggested title: `{{suggested_pr_title}}`
- Suggested branch name if needed: `{{suggested_branch_name}}`
- Batch: `{{batch_id}}`
- Worktree lane: `{{worktree_id}}` (`{{worktree_branch}}`)
- Cloud mode: `{{cloud_mode}}`
- Local acceptance proof required: `{{needs_local_acceptance}}`
- Issue class: `{{issue_class}}`
- Required evidence: `{{evidence_required}}`

## Adapter Status

The repo-native orchestrator can target `{{provider_id}}`, but this slice leaves the Claude adapter as a placeholder contract. Use these notes as the stable PR/update envelope once remote Claude dispatch is wired.

## Reviewer-Facing Evidence

{{reviewer_evidence_expectation}}

## Readiness Note

{{readiness_reason}}
