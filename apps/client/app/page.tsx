export default function HomePage() {
  return (
    <main className="page-shell">
      <section className="card">
        <p className="eyebrow">Tong Client Shell</p>
        <h1>Mobile-first demo shell is ready</h1>
        <p>
          This baseline app shell is intentionally lightweight. Feature-rich overlays and narrative scenes
          are implemented in the `codex/mock-ui` and game-engine workstreams.
        </p>
      </section>
    </main>
  );
}
